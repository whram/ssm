<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!--把对象的创建交给spring来管理-->
    <!--spring对bean的管理细节
        1.创建bean的三种方式
        2.bean对象的作用范围
        3.bean对象的生命周期
    -->
<!--1.创建bean的三种方式-->
    <!--第一种方式在：使用默认构造函数创建
           在spring的配置文件中使用bean标签，配以id和class属性，且没有其他属性和标签
           采用的是默认构造函数创建bean对象的，此时如果类中没有默认构造函数，则对象无法创建
    <bean id = "accountService" class="com.reacher.service.impl.AccountServiceImpl"></bean>
    -->
    <!--第二种方式：使用普通工厂中的方法创建（使用某个类中的方法创建，并存入spring容器）
        在类中没有默认构造方的时候使用
    <bean id="instanceFactory" class="com.reacher.factory.InstanceFactory"></bean>
    <bean id="accountService" factory-bean="instanceFactory" factory-method="getAccountService"></bean>
    -->
    <!--第三种方式：使用静态工厂中的静态方法创建对象（使用某个类中的静态方法创建，并存入spring容器中
        在工厂类中有静态方式时使用
    <bean id="accountService" class="com.reacher.factory.StaticFactory" factory-method="getAccountService"></bean>
    -->

<!--2.bean对象的作用范围-->
    <!--bean中的对象默认是单例的
        bean标签的scope属性：
            作用：用于指定bean的作用范围
            取值：
                singleton：单例（默认值）
                prototype：多例
                request：作用于web应用的请求范围
                session：作用于web应用的会话范围
                global-session：作用于集群作用的会话范围（全局会话范围），当不是集群环境时，它就是session
    <bean id="accountService" class="com.reacher.factory.StaticFactory" factory-method="getAccountService" scope="prototype"></bean>
    -->

<!--3.bean对象的生命周期-->
    <!--
        单例对象：
                出生：容器创建时对象出生
                存活：与容器同在
                死亡：容器销毁，对象销毁
        多例对象：
                出生：当我们使用对象时spring框架为我们创建对象
                存活：对象在使用过程中就一直存活
                死亡：当对象长时间不用且没有别的对象引用时，由java的垃圾回收器回收
    -->
    <bean id="accountService" class="com.reacher.service.impl.AccountServiceImpl"
          scope="prototype" init-method="init" destroy-method="destroy"></bean>
</beans>